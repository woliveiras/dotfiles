#!/bin/bash

## utils

. bin/autoload

## Apps used on System

## Update System

Warning "Adding PPAs"

. bin/system-settings/add-repositoryes

Warning "Updating System"

UpdateSystem

## Essential packages

Warning "Install essentials"

Install "build-essential libssl-dev libcurl4-gnutls-dev libexpat1-dev gettext p7zip p7zip-full p7zip-rar lzma lzma-dev ubuntu-restricted-extras apt-transport-https ca-certificates software-properties-common terminator gnupg"

## Dev essentials

Warning "Dev essentials"

Install "git vim zsh"

git clone https://github.com/VundleVim/Vundle.vim.git ~/.vim/bundle/Vundle.vim

sh -c "$(curl -fsSL https://raw.github.com/robbyrussell/oh-my-zsh/master/tools/install.sh)"

cp -v bin/system-settings/.bashrc bin/system-settings/.gitconfig bin/system-settings/.vimrc $HOME

Warning "Install and Configure NVM and NodeJS (stable and 0.12)"

curl -o- https://raw.githubusercontent.com/creationix/nvm/v0.33.11/install.sh | bash

mkdir ~/.nvm
nvm install stable
nvm install 0.12

## Utils

Warning "Install utils"

Install "gimp inkscape vlc browser-plugin-vlc shutter unetbootin"

## Video utils

Warning "Install video utils"

Install "ffmpeg mint-meta-codecs pitivi kdenlive obs-studio audacity simplescreenrecorder"

## Optimize Ubuntu

Warning "Optimize Ubuntu"

Install "ubuntu-tweak compizconfig-settings-manager preload indicator-keylock gir1.2-gtop-2.0"

## Optimize battery management

Warning "Optimize battery management"

sudo apt-get remove laptop-mode-tools

Install "tlp tlp-rdw"

sudo tlp start

## Remember

Alert "Remember to install later: " "Chrome\n- Slack\n- Java (uurgh)"